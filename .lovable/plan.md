# Refresh the three-level Live Monitor

## Visual system
- Extend the lightweight SVG isometric primitives with campus roads, green areas, trees, outlined buildings, line blocks, wireframe machines, conveyors, status dots, and anchored labels.
- Keep all colors tied to the existing semantic OEE and status tokens, including light and dark themes.

## Multi Plant
- Rebuild the KPI strip as one Group OEE summary followed by Jatake 1, 2, and 4 summaries with donut and A/P/Q bars.
- Replace the simple ground plane with a broad site plan containing roads, plots, landscaping, and three distinct clickable buildings with health-colored outlines and roof labels.

## Plant
- Recompose the upper area into the plant OEE summary, three zone summaries, and a compact alarms table.
- Render a clean factory baseplate with three clearly separated production-zone clusters made from long status-colored line blocks and clickable anchored labels.

## Production Floor
- Preserve the five zone KPI cards and rebuild the map as a dense machine layout with gray bases, translucent upper frames, conveyor connections, one status dot per machine, and status-colored line labels.
- Make lines directly clickable for drill-down while retaining the existing optional detail panel.

## Interaction and layout
- Preserve the reusable wheel zoom, zoom buttons, reset, drag-to-pan, touch behavior, and click-versus-pan safeguards at every level.
- Keep the compass above the transformed scene and make each map fill all remaining Control Tower viewport space.

## Verification
- Test all three views at the supplied large-screen proportions and a compact width.
- Verify wheel zoom, button zoom, reset, pan, plant-to-zone-to-line navigation, active status colors, and browser error logs.
